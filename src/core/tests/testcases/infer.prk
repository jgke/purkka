fun foo() {
    let a = 1;
    let b = a;
    return b;
};

let bar = fun (left, right) {
    let res = left + right;
    return res;
};

let for_each = fun(f: int -> void, list, n) {
    for (let i = 0; i < n; i++) {
        f(list[i]);
    }
};
